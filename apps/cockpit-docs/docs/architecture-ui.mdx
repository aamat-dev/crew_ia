---
sidebar_position: 4
---

# Architecture UI

## AppShell

Structure de base : header, navigation, contenu.

## Providers

Contextes globaux : thème, i18n, requêtes.

## State

Utiliser `React.Context` et `zustand` pour l'état global.

## API

Accès via hooks typed (`useQuery`, `useMutation`).
